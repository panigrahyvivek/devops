_schema-version: "2.0.0"
ID: "devops"
version: 1.0.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "ui5"
    type: html5
    path: .
    parameters:
      version: 1.0.0-${timestamp}
